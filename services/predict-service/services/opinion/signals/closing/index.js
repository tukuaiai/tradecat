/*
 * ############################################################
 * # 📘 文件说明：
 * # 本文件实现的功能：导出扫尾盘信号模块的核心类与格式化方法，便于外部统一引用。
 *
 * # 📋 程序整体伪代码（中文）：
 * # 1. 初始化主要依赖与变量；
 * # 2. 加载输入数据或接收外部请求；
 * # 3. 执行主要逻辑步骤（如计算、处理、训练、渲染等）；
 * # 4. 输出或返回结果；
 * # 5. 异常处理与资源释放；
 *
 * # 🔄 程序流程图（逻辑流）：
 * # ┌──────────┐
 * # │  输入数据 │
 * # └─────┬────┘
 * #       ↓
 * # ┌────────────┐
 * # │  核心处理逻辑 │
 * # └─────┬──────┘
 * #       ↓
 * # ┌──────────┐
 * # │  输出结果 │
 * # └──────────┘
 *
 * # 📊 数据管道说明：
 * # 数据流向：内部模块 → 统一出口 → 上层调用
 *
 * # 🧩 文件结构：
 * # - 模块1：导出 ClosingMarketScanner 功能；
 * # - 模块2：导出 formatClosingSignal 功能；
 *
 * # 🕒 创建时间：2025-10-26 14:00:00
 * # 👤 作者/责任人：Codex Assistant
 * # 🔖 版本：v1.0.0
 * ############################################################
 */

const ClosingMarketScanner = require('./detector');
const { formatClosingSignal } = require('./formatter');

module.exports = {
    ClosingMarketScanner,
    formatClosingSignal
};
